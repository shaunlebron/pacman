//////////////////////////////////////////////////////////////////////////////////////
// Entry Point

window.onload = function() {
    gui.create();
    switchState(menuState);
    executive.init();
};
